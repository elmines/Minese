define fizzbuzz(var x, var limit)
	{

	while (x < 50)
		{
		println("x = ", x);
		if (x == 42) return x;
		if      ( (x % 4) == 0) println("	Fizz");
		else if ( (x % 3) == 0) println("	Buzz");
		else                    println("	...");
		x = x + 2;
		}


	}

define main() {

	var x;
	var y;

	x = y = 3 * 5 + 4;
	println("x = ", x, " y = ", y);

	println(x == y);
	println(x != y);

}
